'use client';
import {useEffect,useRef,useState} from 'react';
export const steps=[['CREATE','Publish your story.'],['DISTRIBUTE','Make it available through a more open distribution ecosystem.'],['CONNECT','Reach audiences and turn discovery into genuine connection.'],['ENGAGE','Give fans access to exclusive content, digital ownership, and experiences.'],['EARN','Receive transparent, real-time rewards while retaining more of the value you create.']];
export default function Journey(){const ref=useRef<HTMLElement>(null);const [active,setActive]=useState(0);useEffect(()=>{const update=()=>{const el=ref.current;if(!el)return;const r=el.getBoundingClientRect();const span=el.offsetHeight-window.innerHeight;setActive(Math.min(4,Math.max(0,Math.floor((-r.top/Math.max(1,span))*5))))};window.addEventListener('scroll',update,{passive:true});update();return()=>window.removeEventListener('scroll',update)},[]);return <section ref={ref} id="how-it-works" className="journey"><div className="journey-sticky wrap"><div className="section-top"><div><p className="eyebrow">03 / HOW IT WORKS</p><h2>From story to audience—<br/><span>more directly.</span></h2></div><span className="small-note">A VIRTUOUS CIRCLE.<br/>NOT A ONE-WAY STREET.</span></div><div className="step-track">{steps.map(([title,copy],i)=><article key={title} className={`step ${i===active?'active':''}`}><div className="step-index"><span>0{i+1}</span><i/></div><h3>{title}</h3><p>{copy}</p></article>)}</div><div className="loop"><span>Creator</span><b>→</b><span>Story</span><b>→</b><span className="lumos">✳ Lumosverse</span><b>→</b><span>Audience</span><b>→</b><span>Superfan</span><b>↴</b><span>Creator ↺</span></div><div className="journey-bottom"><div className="tech"><span>Decentralized distribution</span><span>Tokenized payouts</span><span>Digital ownership</span><span>Superfan experiences</span></div><a className="text-link" href="/how-it-works">See How It Works <span>↗</span></a></div></div></section>}
